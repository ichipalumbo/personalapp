# C-Fix — reparo da Rodada C

## 1) git diff --stat

```text
assets/js/shared/recurrence-helpers.js  |  7 ++-----
backend/src/services/gcalSyncService.js | 27 ++++++++++++++++++++-------
backend/test/gcal-sync.test.js          | 33 +++++++++++++++++++++++++++++++--
3 files changed, 53 insertions(+), 14 deletions(-)
```

## 2) Status dos itens (1 a 4)

- Item 1 — feito. Restaurei as 11 guardas corrompidas em `backend/src/services/gcalSyncService.js` e mantive a única linha legítima do `COUNT` no bloco `montarRecurrence`.
- Item 2 — feito. Mantive o `UNTIL` no formato básico RFC 5545 (`YYYYMMDDT235959Z`) e atualizei os testes que esperavam a forma estendida.
- Item 3 — feito. Removi o filtro de exceção da contagem local em `assets/js/shared/recurrence-helpers.js`, preservando ainda a filtragem em `checarCompromissoNaData`.
- Item 4 — feito. A cobertura nova foi validada por mutação cirúrgica e o suíte final ficou verde.

## 3) Saída do script do item 1 e contagem final da linha do COUNT

```text
OK: 11 guardas restauradas
```

Contagem final do COUNT no arquivo de serviço: `1`.

## 4) Antes e depois do `UNTIL`

Antes (formato estendido):

```text
2026-08-27T23:59:59Z
2026-02-28T23:59:59Z
```

Depois (formato básico RFC 5545):

```text
20260827T235959Z
20260228T235959Z
```

Testes ajustados:

- `montarRecurrence gera RRULE semanal com BYDAY, INTERVAL e UNTIL em UTC`
- `montarRecurrence monta COUNT e monthOfDate sem combinar UNTIL`

## 5) Tabela de mutação dos testes novos

| Teste novo | Linha revertida em mutação | Resultado observado |
| --- | --- | --- |
| `montarRecurrence devolve null quando a data de início é inválida` | `if (!dataInicio) { return null; }` no ramo mensal | Falhou com `actual: ['RRULE:FREQ=MONTHLY;BYMONTHDAY=1']` e `expected: null` |
| `montarRecurrence gera RRULE semanal ... UNTIL` | `return date.toISOString().replace(...).replace(...).replace(...);` em `formatarDataUtcRfc5545` | Falhou com `UNTIL=2026-08-27T23:59:59.000Z` em vez de `UNTIL=20260827T235959Z` |
| `count de recorrencia inclui excecoes sem reduzir a contagem` | Reintroduzi `if (comp.excecoes.includes(dataStr)) continue;` em `contarOcorrenciasAteData` | Falhou com `true !== false` para a data 01/09/2026 |

## 6) Item 3: mudança financeira?

Nenhum teste financeiro mudou de resultado. A correção da contagem local alinha a série com o Google sem reabrir ciclos pagos; a regra de snapshot e ciclo fechado continua preservada.

## 7) Saída final do `npm test`

```text
ℹ tests 72
ℹ suites 0
ℹ pass 72
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 893.7713
```

## 8) O que encontrei e não alterei por estar fora de escopo

- Não alterei `backend/src/models/Agendamento.js`.
- Não alterei `assets/js/view-financas.js` nem a lógica financeira de UI.
- Não alterei `backend/src/services/financasService.js` nem qualquer cálculo de ciclo pago, porque o item 3 era apenas de contagem local do motor de recorrência.
- Não alterei nenhum fluxo de Google Calendar fora do RRULE/EXDATE necessário para esta correção.
